class Dish {
  name: string
  description: string
  image: string
  id: number

  constructor(name: string, description: string, image: string, id: number) {
    this.name = name
    this.description = description
    this.image = image
    this.id = id
  }
}

export default Dish
